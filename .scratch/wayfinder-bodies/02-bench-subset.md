## Question

Which Terminal-Bench tasks (target ~10–20, adjustable by model speed) form CLAI’s primary scored subset, and what minimal custom/hidden tasks (if any) sit beside them for the eval report?

## Constraints

- Terminal-Bench is the PS primary family; SWE-bench etc. are complements only if time allows.
- Must support reproducible runner + metrics in items 24–26.
- Prefer tasks that fit Docker/`sandbox-runtime` and our LSP-present eval assumption.
