## Question

Scaffold the CLAI TypeScript workspace per the locked package layout: AI SDK adapter stub, Ink shell, tool stubs, SQLite memory stub, JSONL run writer, AGENTS.md with conventional commit rules — so implementation tickets have a home.

## Done when

- `pnpm`/`npm` install + `clai --help` (or chosen binary) works.
- AGENTS.md documents commit prefixes and wayfinder map link.
- Empty modules exist at the seams named in the layout ticket.
