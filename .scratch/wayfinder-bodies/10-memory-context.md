## Question

Implement memory + context manager per `memory-context-architecture.md` v2 (SQLite tiers, provenance, invalidate/supersede, 4-stage assemble, ablation gates, injection labels).

## Done when

- Memory inspect/edit/export/delete CLI works.
- Ablation flags change assemble behavior.
- Red-team README injection demo produces a clean refusal/non-compliance trace note.
