## Question

Ship reproducible benchmark runner for the chosen subset, metrics table (item 25), ablation switches (item 26), and scripts to run Config A baselines vs CLAI under a shared budget.

## Done when

- One command runs the subset and writes a result table + at least one failure case.
- Baseline comparison can be invoked once Config A is chosen.
