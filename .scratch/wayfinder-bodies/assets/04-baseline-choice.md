# Config A baseline choice

**Scored baselines (primary table):** OpenCode, Pi — same model / same budget where each harness allows it. Comparison row: CLAI vs OpenCode vs Pi.

**Named only (optional later; not in primary scored table for now):** Codex CLI, Cursor CLI.

Attribution: harness delta, not model delta.
