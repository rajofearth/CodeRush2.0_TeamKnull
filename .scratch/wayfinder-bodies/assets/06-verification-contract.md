# Verification completion contract

**Follow OpenCode / Pi where feasible.**

- Completion is session/model-declared (soft), not a hard harness finish lock.
- Agent runs tests/lint/build via tools when warranted; record evidence when run.
- One bounded repair edge for demos/task-graph (`verify → repair → reverify`).
- Scored Terminal-Bench: Harbor/TB verifier is ground truth.
