{
  "id": "add-graceful-shutdown",
  "title": "Drain hooks on shutdown signal",
  "prompt": "ShutdownManager in shutdown.mjs runs registered async hooks in order when shutdown() is called. Verify with `node check.mjs` (exit code 0 means done).",
  "category": "feature",
  "timeoutMs": 150000,
  "maxSteps": 12,
  "source": {
    "benchmark": "deepswe",
    "taskId": "graceful-shutdown-hooks"
  }
}
