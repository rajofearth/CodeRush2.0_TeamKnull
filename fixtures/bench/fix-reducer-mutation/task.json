{
  "id": "fix-reducer-mutation",
  "title": "Fix reducer mutating accumulator",
  "prompt": "totalByKey(items) in totals.mjs must not mutate the seed object across calls. Verify with `node check.mjs` (exit code 0 means done).",
  "category": "bugfix",
  "timeoutMs": 150000,
  "maxSteps": 12,
  "source": {
    "benchmark": "terminal-bench",
    "taskId": "fix-code-vulnerability"
  }
}
