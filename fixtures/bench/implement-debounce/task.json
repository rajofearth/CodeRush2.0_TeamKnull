{
  "id": "implement-debounce",
  "title": "Debounce function calls",
  "prompt": "debounce(fn, waitMs) in debounce.mjs delays invocation until waitMs after last call. Verify with `node check.mjs` (exit code 0 means done).",
  "category": "feature",
  "timeoutMs": 150000,
  "maxSteps": 12,
  "source": {
    "benchmark": "deepswe",
    "taskId": "debounce-helper"
  }
}
