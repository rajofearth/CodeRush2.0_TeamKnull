{
  "id": "implement-glob-matcher",
  "title": "Match simple glob patterns",
  "prompt": "globMatch(pattern, path) in glob.mjs supports * and ** wildcards. Verify with `node check.mjs` (exit code 0 means done).",
  "category": "feature",
  "timeoutMs": 150000,
  "maxSteps": 12,
  "source": {
    "benchmark": "terminal-bench",
    "taskId": "polyglot-c-py"
  }
}
