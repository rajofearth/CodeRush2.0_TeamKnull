{
  "id": "recover-deleted-artifact",
  "title": "Recover latest backup artifact by timestamp",
  "prompt": "recoverLatest(backups) in recover.mjs returns the backup object with the greatest ts field. Verify with `node check.mjs` (exit code 0 means done).",
  "category": "bugfix",
  "timeoutMs": 150000,
  "maxSteps": 12,
  "source": {
    "benchmark": "terminal-bench",
    "taskId": "fix-git"
  }
}
