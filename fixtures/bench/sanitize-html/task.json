{
  "id": "sanitize-html",
  "title": "Strip script tags and event handlers from HTML",
  "prompt": "Implement sanitizeHtml(html) in sanitize.mjs. Remove <script> blocks, on* attributes, and javascript: URLs. Keep safe text and tags. Verify with `node check.mjs` (exit code 0 means done).",
  "category": "feature",
  "timeoutMs": 150000,
  "maxSteps": 12,
  "source": {
    "benchmark": "terminal-bench",
    "taskId": "filter-js-from-html"
  }
}
